package weapons;

public class MilitaryRocket extends Rocket {
    public MilitaryRocket(int range, int altitude, String direction, int x, int y) {
        super(range, altitude, direction, x, y);
    }
}
